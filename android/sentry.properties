
# auth.token is intentionally omitted here.
# The Sentry Gradle plugin reads SENTRY_AUTH_TOKEN from the environment automatically.
# In CI, set the SENTRY_AUTH_TOKEN secret in GitHub Actions secrets.

defaults.org=kirito-9p
defaults.project=react-native

defaults.url=https://sentry.io/
