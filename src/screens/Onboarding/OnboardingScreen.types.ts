export interface OnboardingData {
  id: string;
  animation: any; // Adjust type based on Lottie animation source (e.g., AnimationObject from lottie-react-native)
  title: string;
  subtitle: string;
}

export interface OnboardingScreenProps {}
